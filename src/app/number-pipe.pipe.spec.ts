import { NumberPipePipe } from './number-pipe.pipe';

describe('NumberPipePipe', () => {
  it('create an instance', () => {
    const pipe = new NumberPipePipe();
    expect(pipe).toBeTruthy();
  });
});
